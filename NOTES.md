# Notes

- Langue : toutes les leçons et documents en français.
- Setup : Nicolas rédige/planifie sous Windows (Claude Code) et build/run réel sur un Mac séparé avec Xcode. Le pont entre les deux est Git — ce dossier `karamock` est un dépôt lié à `https://github.com/Njko/karamock`. Il doit cloner ce dépôt sur son Mac pour créer et faire évoluer le vrai projet Xcode à côté du contenu pédagogique.
- Règle explicite (2026-07-26) : ne jamais committer ni pousser de code projet (`Sources/Karamock/...`) depuis cette machine Windows — uniquement les leçons (`lessons/*.html`) et documents associés (`NOTES.md`, `learning-records/*.md`, `MISSION.md`). Si une incohérence est repérée dans `Sources/`, la signaler en discussion et laisser Nicolas corriger/commiter sur le Mac plutôt que d'éditer et pousser le code Swift depuis Windows.
- Périmètre confirmé avec Nicolas : UI/navigation uniquement, données factices, pas de vrai moteur audio ni de vraie synchronisation de paroles (voir MISSION.md → Out of scope).
- Retour explicite (2026-08-03) : retirer toute référence à "Tech Lead" des blocs "Pourquoi cette leçon" — les leçons sont censées être lisibles par d'autres profils que Nicolas, pas seulement raccrochées à son expérience/poste visé. 13 leçons corrigées (6, 24-35 sauf le second bloc `mission-tie` de la Leçon 34, qui est un statut d'implémentation, pas un "pourquoi"). Les leçons 1-5, 7-23 utilisaient déjà un autre angle ("MISSION.md", "l'audit", théorie vs pratique) et n'ont pas été touchées. Vérifier ce point pour chaque futur bloc "Pourquoi cette leçon".
- Mission volontairement séparée d'un autre espace de travail théorique sur SwiftUI/Swift 6 — celle-ci est 100% pratique. Les deux se nourrissent mutuellement mais ne partagent pas les mêmes learning-records.
- Captures de référence de l'app KaraFun dans `reference/screenshots/` (fournies par Nicolas) : 01-decouvrir (écran d'accueil), 02-playlist-detail (liste de chansons "Les Légendes du Rock"), 03-fiche-chanson (options Karaoké/Battle, tonalité/tempo), 04-lecteur-plein-ecran (paroles + mini-lecteur).
- Premier commit poussé sur GitHub ; contenu des leçons 1 à 6 poussé le 2026-07-23 (fusionné avec le vrai projet Xcode que Nicolas pousse depuis son Mac au fur et à mesure, dans `Sources/Karamock/`).
- Programme confirmé pour la suite (mis à jour 2026-07-26 après insertion de la Leçon 8 de nettoyage), à ajuster si Nicolas change d'avis :
  - Leçon 7 — Domain/UseCase : `DownloadSongUseCase`, justifié par la règle "une chanson téléchargée doit apparaître dans la Bibliothèque" (LibraryView, stub vide depuis la Leçon 1). Fait.
  - Leçon 8 — Nettoyage : retrait de `service: SongDownloading` de `SongDownloadViewModel`, absorbé dans `DownloadSongUseCase` (Dependency Rule, repéré par Nicolas). Fait.
  - Leçon 9 — L'onglet Bibliothèque affiche les chansons téléchargées (état partagé, pas mockPlaylists figé). `LibraryViewModel` lit via `FetchDownloadedSongsUseCase` (pas d'accès direct au Repository, corrigé par Nicolas avant même de commencer l'exercice — cf. Dependency Rule de la Leçon 8). Fait.
  - Leçon 10 — Composition Root : déplace la construction de `InMemoryDownloadedSongsRepository()` de `RootTabView` (une View) vers `KaramockApp` (`@main App`), seul endroit autorisé à connaître les types concrets. Décision volontaire de NE PAS introduire de conteneur `AppDependencies` pour l'instant (seulement 2 dépendances partagées, un conteneur serait une abstraction prématurée) — à réévaluer si le graphe grossit. Couvre aussi le piège de la valeur par défaut de `@Entry` (pas de crash si l'injection est oubliée au composition root), et un bug réel corrigé pendant la rédaction (`$player.isExpanded` → `Bindable(player).isExpanded`, `@Environment` n'a pas de projection `$`). Écrite, pas encore faite par Nicolas.
  - Leçon 11 — Refactoring du composition root manuel vers Factory (librairie de DI compile-time safe, `import FactoryKit`). Nicolas a d'abord demandé Swinject, j'ai recherché et recommandé Factory à la place (Swinject = résolution runtime, adapté aux gros projets UIKit existants ; Factory = vérifié à la compilation, pensé pour SwiftUI/Swift 6, recommandation actuelle pour un projet neuf) — Nicolas a suivi la recommandation après argumentation. Voir `learning-records/0007-...`. Écrite, pas encore faite par Nicolas.
  - Leçon 12 — Les ViewModels rejoignent le Container (`ParameterFactory<Song, SongDownloadViewModel>` pour le paramètre par instance, `@InjectedObservable` pour `LibraryViewModel`). Nicolas a repéré lui-même que `SongOptionsSheet`/`LibraryView` injectaient encore un UseCase et assemblaient le ViewModel à la main en Leçon 11 — cette leçon referme cette fuite de couche. Limite documentée par Factory lui-même : une `ParameterFactory` ne peut pas être injectée via un property wrapper (pas de moyen de lui passer le paramètre avant l'init), donc `SongOptionsSheet` continue d'appeler `Container.shared` directement pour ce seul cas. Écrite, pas encore faite par Nicolas.
  - Leçon 13 — Tests (Swift Testing), rendus faciles par le découplage des Leçons 6-12 (Factory facilite le mocking via `Container.shared.x { Mock() }`). Fait.
  - Leçon 14 — Accessibilité VoiceOver : `accessibilityLabel` sur les 4 boutons icône-seule de `MiniPlayerBar`/`FullScreenPlayerView` (play/pause dynamique, fermer, précédent/suivant). Argument spécifique au projet : l'étiquette système d'un SF Symbol arrive dans la langue système (généralement anglais), alors que toute l'UI de Karamock est en français. Motivation volontairement formulée comme une responsabilité d'équipe (design/product/QA), pas seulement des devs — retour explicite de Nicolas. Aside teasant le piège du `.onTapGesture` sur `MiniPlayerBar` pour la leçon suivante. Fait.
  - Leçon 15 — VoiceOver et le bouton imbriqué : corrige le `.onTapGesture` de `MiniPlayerBar` (pas de trait .isButton ni d'action d'accessibilité par défaut). Le vrai piège : ne pas combiner tout le HStack (`.accessibilityElement(children: .combine)` sur l'ensemble avalerait le bouton play/pause imbriqué) — grouper seulement la pochette+texte, laisser le bouton play/pause en dehors. `.accessibilityHidden(true)` sur le rectangle de pochette décoratif. Nicolas a challengé la justification initiale ("pourquoi le texte deviendrait un bouton ?") — réécrite en partant de l'objectif de parité (mêmes deux gestes indépendants qu'un utilisateur voyant), avec un aside sur l'alternative rotor écartée. Fait.
  - Leçon 16 — Dynamic Type : le bouton play/pause central de `FullScreenPlayerView` utilise `.font(.system(size: 64))` (taille fixe, ne grossit pas), contrairement aux boutons précédent/suivant voisins en `.font(.title)` (style sémantique, grossit normalement). Correction via `@ScaledMetric(relativeTo: .largeTitle)`. Argument spécifique au projet : usage karaoké à distance / lecture des paroles sans lunettes, pas un cas limite. Aside clarifiant que le cadre fixe 160pt de `LyricsView` n'est PAS le même bug (déjà un ScrollView, rien n'est perdu). Fait.
  - Leçon 17 — Dynamic Type, audit final : `SongRow` (rien à corriger, rien n'est masqué), `PlaylistCard` (vraie régression — `.clipShape` coupe net le titre en `.headline` à un réglage de texte élevé, faute de `.lineLimit`/`.dynamicTypeSize`), `PlaylistCoverHeader` (problème différent — pas de `.clipShape`, donc chevauchement visuel plutôt que découpe, laissé en exercice pour Nicolas). Correction de `PlaylistCard` : `.lineLimit(2)` + `.dynamicTypeSize(...DynamicTypeSize.accessibility1)` + `.accessibilityShowsLargeContentViewer` pour restituer le titre complet. Aside réconciliant avec la Leçon 16 (plafonner n'est légitime que pour un élément à mise en page contrainte, jamais pour éviter un vrai problème de layout). Écrite, pas encore faite par Nicolas. Clôt l'audit Dynamic Type ouvert en Leçon 16.
  - Leçon 18 — Header extensible de `PlaylistDetailView` (effet "stretchy" façon Spotify/Apple Music). Née d'un test réel de Nicolas sur la Leçon 17 : le header n'était ni dans le scroll, ni sous la barre de statut. Fusion `List` → `ScrollView`+`VStack` (header et chansons dans un seul contenu défilant), `.ignoresSafeArea(edges: .top)`, puis un modificateur réutilisable `stretchy()` (`Views/Common/View+Stretchy.swift`) via `visualEffect` + `geometry.frame(in: .scrollView)` + `scaleEffect(anchor: .bottom)`. N'affecte pas la correction Dynamic Type de la Leçon 17 (ZStack inchangé). Écrite, pas encore faite par Nicolas.
  - Leçon 19 — Concurrence (1/N) : correction Swift 6 faite par Nicolas sur son Mac le 2026-07-27, avant même de commencer la Leçon 18 (`fix : correction de configuration projet pour Swift 6`). Le projet a `SWIFT_APPROACHABLE_CONCURRENCY = YES` et `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` (confirmé dans `project.pbxproj`, cf. SE-0466) : toute déclaration non annotée du module devient implicitement `@MainActor`. Important, corrigé par Nicolas après une première version erronée de la leçon : `SimpleDownloadedSongsRepository` était déjà un `actor` depuis les leçons précédentes, ce n'est pas lui qui a changé de nature — en cherchant à faire compiler, Nicolas est passé un temps par une version en `final class` + `nonisolated(unsafe) private var storage` (tentative explorée puis écartée, pas l'état de départ ni l'état final). Le vrai correctif : `nonisolated` ajouté sur le protocole `DownloadedSongsRepository` et sur les `init` de `DownloadSongUseCase`/`FetchDownloadedSongsUseCase`, nécessaire car ces types `Sendable` seraient sinon enfermés sur MainActor par le réglage par défaut, en contradiction avec leur nature `Sendable` (cf. le fil forums.swift.org cité en source). Conséquence : `@MainActor` retiré des Factory `downloadSong`/`fetchDownloadedSongs` dans `KaramockContainer.swift`, devenu inutile. `@MainActor` ajouté explicitement sur `PlayerState` par clarté d'intention. Fait (par Nicolas, avant la leçon — la leçon documente après coup un correctif déjà appliqué).
  - Leçon 20 — Interface (2/N) : bug réel repéré par Nicolas — en consultant une playlist pendant qu'une chanson joue, le dernier `SongRow` de `PlaylistDetailView` est caché derrière le mini-lecteur. Cause : `RootTabView` positionne `MiniPlayerBar` avec `.overlay(alignment: .bottom)` + `.padding(.bottom, 49)` (hauteur de barre d'onglets estimée à la main) — un overlay ne réserve pas d'espace, le ScrollView en dessous ignore son existence. Correctif enseigné : `.tabViewBottomAccessory(isEnabled:content:)` (l'API native pensée pour un mini-lecteur au-dessus de la barre d'onglets, cas d'usage documenté explicitement par Apple). Corrigé une première fois par Nicolas après test réel : conditionner uniquement le *contenu* (`if let song = …`) ne suffit pas — l'accessoire reste réservé comme zone vide si le TabView n'est pas informé qu'il faut le retirer. Le paramètre `isEnabled` (ajouté en iOS 26.2, projet ciblant 26.5) résout ça explicitement en retirant l'accessoire de la mise en page, pas seulement son contenu. Troisième correction, après test réel par Nicolas : un fond gris tronqué apparaissait, causé par `MiniPlayerBar` qui pose son propre `.background(.regularMaterial)` + `.clipShape(RoundedRectangle(cornerRadius: 10))`, pensés pour l'ancien `.overlay` flottant — l'accessoire fournit déjà son propre fond Liquid Glass en capsule (confirmé par la session WWDC25 "Build a SwiftUI app with the new design" : ne pas poser son propre fond, le retirer s'il existe déjà). Restylage enseigné : ne garder que la mise en page du contenu (HStack pochette/titre-artiste/bouton), retirer background/clipShape/padding devenus inutiles. Aside sur `tabViewBottomAccessoryPlacement` (`.inline`/`.expanded`) laissé comme piste d'amélioration non traitée (version compacte du mini-lecteur quand la barre d'onglets se réduit). Fait — les trois corrections (overlay→accessory, isEnabled, restylage sans fond propre) sont appliquées dans `Sources/` par Nicolas, code vérifié conforme à la leçon.
  - Leçon 21 — Interface (3/N) : suite directe de la Leçon 20, à partir de son aside laissée ouverte. `MiniPlayerBar` lit `@Environment(\.tabViewBottomAccessoryPlacement)` (`TabViewBottomAccessoryPlacement?` — `.expanded`, `.inline`, ou `nil`) pour basculer entre un contenu complet (pochette+titre+artiste+bouton, état `.expanded`) et un contenu compact (bouton play/pause seul, état `.inline`, quand la barre d'onglets se réduit pendant le scroll). Ce basculement est piloté par `.tabBarMinimizeBehavior`. Ajusté après retour de Nicolas : `.automatic` (la valeur par défaut) ne donne aucun moyen fiable de savoir quand tester le mode compact — remplacé par `.tabBarMinimizeBehavior(.onScrollDown)` explicite sur le TabView, pour un déclenchement reproductible. Scénario de vérification concret ajouté, appuyé sur la playlist mock "Best Of Rock" (trentaine de chansons, `MockPlaylist.swift`, largement de quoi remplir l'écran et déclencher le scroll) : lancer une lecture, ouvrir cette playlist, scroller pour voir la bascule vers compactContent, remonter pour voir le retour à expandedContent. Nouveau composant CSS réutilisable ajouté : `.verify-scenario` dans `assets/style.css` (bordure verte, distincte de `.mission-tie` et `.aside`), pour ce type d'étapes de test concret dans les futures leçons. Deux asides : pourquoi ne pas juste rétrécir pochette/texte plutôt que les retirer (lisibilité), et ce que ça change pour VoiceOver (l'action d'expansion du titre disparaît en compact, pas une régression). Point laissé ouvert : LibraryView dépend des chansons réellement téléchargées par Nicolas, pas d'une donnée mock fixe — pas garanti d'avoir assez de contenu pour le même scénario sur cet onglet. Écrite, pas encore faite par Nicolas.
  - Programme reformulé (décidé 2026-07-28, à la demande explicite de Nicolas) : les pistes de polish plus profond de l'interface — barre de navigation transparente au scroll (teasée en Leçon 18), contraste de couleurs, VoiceOver sur d'autres écrans que le lecteur, Reduce Motion, sections Quiz/Styles de l'écran Découvrir (annoncées Leçon 1, jamais construites) — sont mises de côté, pas abandonnées : à reprendre après les deux nouveaux axes ci-dessous, jugés plus prioritaires par Nicolas.
  - Nouvel axe — Réseau (URLSession) : consommer un vrai service public, quitte à inventer une fonctionnalité originale collée au thème karaoké plutôt que de forcer un service existant dans le mock actuel. Candidats identifiés (réels, sans clé d'authentification, vérifiés le 2026-07-28 — à re-vérifier précisément, forme exacte des requêtes/réponses, au moment d'écrire la leçon) : `api.lyrics.ovh` (paroles réelles d'un titre à partir d'un artiste + titre, remplacerait `mockLyrics` dans `FullScreenPlayerView`/`LyricsView` — idée la plus alignée avec le thème karaoké) et l'iTunes Search API (`itunes.apple.com/search`, métadonnées + artwork réels). Les deux sont combinables : rechercher un vrai titre via iTunes Search, puis récupérer ses vraies paroles via lyrics.ovh — à confirmer avec Nicolas avant de s'engager sur la fonctionnalité exacte.
  - Nouvel axe — Gestion des erreurs : leçon dédiée et distincte, à placer après la leçon réseau puisqu'elle s'appuiera sur les erreurs concrètes que l'appel réseau introduit (URLError, échec de décodage JSON, statuts HTTP). Sujet : comment gérer proprement les erreurs dans une app industrielle (typage des erreurs, distinction recouvrable/non-recouvrable, présentation à l'utilisateur vs journalisation) plutôt que les motifs déjà écartés dans ce projet (pas de `try!`/erreurs avalées silencieusement).
  - Leçon 22 — Réseau (1/N) : décision validée avec Nicolas le 2026-07-28 — utiliser `api.lyrics.ovh` (pas de clé, réponses vérifiées en direct via curl : succès `{"lyrics": "…"}`, 404 `{"error": "No lyrics found"}`) pour remplacer `mockLyrics` par de vraies paroles, sans inventer de nouvelle fonctionnalité : les chansons du mock (`MockPlaylist.swift`) sont déjà de vrais titres (Queen, Guns N' Roses, Eagles…). Architecture corrigée le 2026-07-28 (retour de Nicolas : il manquait un Repository entre le UseCase et le Service, contrairement à la Leçon 7) : `LyricsFetching` protocole + `URLSessionLyricsFetching` (`Services/`, miroir de `SongDownloading`/`MockSongDownloading`, fait uniquement l'appel réseau brut) → `LyricsRepository` protocole + `CachedLyricsRepository` (un `actor`, mémorise les `[LyricsLine]` déjà obtenues par `Song.ID` et ne rappelle le service qu'en cas de cache manquant — même rôle de rétention que `DownloadedSongsRepository`) → `FetchLyricsUseCase` (`Domain/Use Case/`, ne connaît plus que le repository, forme identique à `FetchDownloadedSongsUseCase`). C'est le repository qui convertit le texte brut en `[LyricsLine]`, pas le UseCase. Enregistrés dans `KaramockContainer.swift` (`lyricsFetching`, `lyricsRepository` en `.singleton`, `fetchLyrics`, sans `@MainActor`, cohérent avec la Leçon 19). Seconde correction le 2026-07-28 (retour de Nicolas : la vue appelait le UseCase elle-même, sans passer par un ViewModel, contrairement à `SongDownloadViewModel`/`LibraryViewModel` partout ailleurs dans le projet) : nouveau `FullScreenPlayerViewModel` (`@MainActor @Observable final class`, `song`+`fetchLyrics` injectés au constructeur, `lyrics`+`loadLyrics()`), enregistré en `ParameterFactory<Song, FullScreenPlayerViewModel>` (`fullScreenPlayerViewModel`, même schéma que `songDownloadViewModel`). `FullScreenPlayerView` gagne `@State private var viewModel: FullScreenPlayerViewModel?`, résolu dans `.task(id: song.id)` via `Container.shared.fullScreenPlayerViewModel(song)` (nécessaire car `RootTabView` présente cet écran via `.fullScreenCover(isPresented:)`, pas `.fullScreenCover(item:)` — la même instance de vue peut recevoir une chanson différente sans être refermée). `LyricsView` n'est pas modifiée. Piège traité : `addingPercentEncoding(withAllowedCharacters: .urlPathAllowed)` nécessaire pour les espaces/apostrophes des vrais titres ("Sweet Child O' Mine"). Compromis assumé et documenté : lyrics.ovh ne fournit aucun horodatage par ligne, donc le surlignage synchronisé de `LyricsView` devient une approximation (lignes réparties uniformément sur `song.durationInSeconds`), pas une vraie synchro karaoké. Gestion d'erreur volontairement sommaire (un seul catch générique, message de repli) — sujet explicitement renvoyé à la Leçon 23. Scénario de vérification concret ajouté (`.verify-scenario`) : tester avec "Bohemian Rhapsody" de Queen (vérifiée fonctionner), rouvrir la même chanson Wi-Fi coupé pour vérifier le cache, une association artiste/titre improbable, et une chanson jamais vue avec le Wi-Fi coupé. Cinquième question de quiz ajoutée sur le rôle du repository, sixième sur le rôle du ViewModel. Comparée le 2026-07-28 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 22`) : architecture identique à ce qui était enseigné (`LyricsFetching`/`URLSessionLyricsFetching`, `LyricsRepository`/`CachedLyricsRepository`, `FetchLyricsUseCase`, `FullScreenPlayerViewModel`, Container). Deux ajustements faits d'après le vrai code : `lyricsURL` est `private` (vit dans `LyricsFetching.swift`) ; et un vrai correctif de concurrence rencontré à l'implémentation a été documenté en aside — `Song.durationInSeconds` (`Views/Common/TimeUtilities.swift`) a dû devenir `nonisolated`, car `CachedLyricsRepository.lyrics(for:)` y accède sans `await` depuis l'acteur du repository, alors que `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` (Leçon 19) l'isolait implicitement au `MainActor`. Leçon désormais conforme à l'implémentation. Précision ajoutée à la demande de Nicolas : `lyricsURL` doit pour le moment rester dans le même fichier que le service qui l'utilise (`LyricsFetching.swift`), pas un choix figé — même logique que l'aside sur l'arborescence des dossiers en Leçon 7.
  - Leçon 23 — Gestion des erreurs (1/N) : écrite le 2026-07-28, suite directe de la Leçon 22 (dont elle corrige explicitement le catch générique sommaire, annoncé comme provisoire). Modélise `LyricsError` (`enum: Error, Sendable`, cas `invalidURL`/`notFound`/`network`/`malformedResponse`, sans donnée associée — volontairement, pour rester trivialement `Sendable` et ne pas transporter le diagnostic technique à travers les couches), suggéré à côté de `LyricsLine` dans `Views/Common/Model/`. Utilise les "typed throws" de Swift 6 (SE-0413, vérifié via la proposition Swift Evolution) : `throws(LyricsError)` propagé de bout en bout — `LyricsFetching`/`URLSessionLyricsFetching` (interprète désormais explicitement le code HTTP de lyrics.ovh : 404 → `.notFound`, échec réseau → `.network`, décodage JSON raté → `.malformedResponse`, chacun journalisé via `os.Logger` sauf `.notFound` qui est un signal métier normal, pas un bug) → `LyricsRepository`/`CachedLyricsRepository` → `FetchLyricsUseCase`, tous en `throws(LyricsError)` sans conversion. `FullScreenPlayerViewModel.loadLyrics()` fait désormais un `switch` exhaustif sur `LyricsError` avec trois messages distincts (introuvable / connexion impossible / repli générique) au lieu d'un seul message pour tout. Nav de la Leçon 22 mise à jour pour pointer vers cette leçon. Comparée le 2026-07-28 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 23`) : architecture identique à ce qui était enseigné (typed throws de bout en bout, interprétation du code HTTP dans le Service, `.notFound` jamais journalisé). Trois ajustements faits d'après le vrai code : `LyricsError` vit réellement dans `Services/LyricsError.swift` (pas `Views/Common/Model/` comme suggéré) ; le logger réel s'appelle `lyricsFetcherLogger`, subsystem `fr.nicolaslinard.karamock`, et journalise le `HTTPURLResponse` complet (pas seulement `statusCode`) sur un statut inattendu ; les messages affichés à l'utilisateur diffèrent légèrement du texte enseigné ("Paroles indisponibles pour cette chanson." pour `.notFound`, "Connexion impossible. Réessayez" pour `.network`) — corrigés dans la leçon et dans son scénario de vérification. Leçon désormais conforme à l'implémentation.
  - Nicolas a fait auditer `Sources/Karamock/` via GitHub Copilot (5 agents spécialisés) le 2026-07-28, poussé dans `audits/2026-07-28-clean-architecture-audit.md` (467 lignes, non modifié par cette session — lu seulement). Note globale 5.9/10. Points clés : Domain quasi-vide (entités dans `Views/`), `Playlist` importe SwiftUI, Data → Views via `TimeUtilities.swift` (DIP), protocoles Domain définis dans Data/Services (DIP), deux bugs prod réels (Task orpheline dans `DownloadSongUseCase` qui marque une chanson téléchargée même après annulation ; `Song.id = UUID()` régénéré à chaque instanciation, déduplication cassée), `MockSongDownloading` câblé en dur en prod, mapping texte→`[LyricsLine]` dans le Repository (SRP), `mockLyrics`/`MockSongDownloading` dans le bundle prod, `DiscoveryView`/`SongOptionsSheet` sans ViewModel + `Container.shared` appelé direct depuis des Views, couverture de tests insuffisante. Roadmap propre en 3 phases dans le fichier — non suivie telle quelle, reformulée en arc de leçons ci-dessous à la demande de Nicolas ("Prépare les prochaines leçons sur la résolution des problèmes remontés par l'audit... nettoyer l'application pour en garder une architecture propre et claire").
  - Programme reformulé (décidé 2026-07-28, à la demande de Nicolas) — arc "Nettoyage architecture (post-audit)", groupé par thème plutôt que par les phases de l'audit (qui sont organisées par effort/risque, pas par concept pédagogique) ; feature-complétude hors périmètre (boutons prev/next, placement du `.sheet`) — ce n'est pas de l'architecture :
    - Leçon 24 (écrite, implémentée) — Domain models hors des Views : Song/LyricsLine/KaraokeMode/LyricsError → `Domain/Models/`, `Playlist` perd `import SwiftUI`/`gradientColors` (déplacé en extension côté Views, mapping sur `title`), `TimeUtilities.swift` scindé en `Domain/Extensions/Song+Duration.swift` + `Views/Common/TimeFormatter.swift` (corrige la dépendance Data → Views). Risque quasi nul, aucun changement de comportement. Comparée le 2026-07-30 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 24`) : conforme trait pour trait à ce qui était enseigné (tous les déplacements de fichiers, `Playlist+GradientColors.swift`, `MockPlaylist.swift`, `Song+Duration.swift`, `TimeFormatter.swift` identiques au code de la leçon) — aucune correction nécessaire, première fois que l'implémentation ne divergeait sur aucun point.
    - Leçon 25 (écrite) — Inverser la dépendance (DIP) : extraction des 4 protocoles (`LyricsRepository`, `DownloadedSongsRepository`, `LyricsFetching`, `SongDownloading`) depuis leurs fichiers d'implémentation (Data/Services) vers `Domain/Interfaces/`, chacun dans son propre fichier ; les implémentations concrètes (`CachedLyricsRepository`, `SimpleDownloadedSongsRepository`, `URLSessionLyricsFetching`, `MockSongDownloading`) restent dans `Repositories/`/`Services/`. `LyricsFetching` gagne le `nonisolated` qui lui manquait (audit 3.7), pour s'aligner sur ses 3 protocoles voisins déjà `nonisolated protocol : Sendable` depuis la Leçon 7. Mécanisme désormais clarifié (2026-07-30, suite à une question de Nicolas en chat, réponse ajoutée dans la leçon) : sous `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`, toute déclaration non annotée — y compris les requirements d'un protocole — est implicitement `@MainActor`. `LyricsRepository`/`DownloadedSongsRepository` ont besoin de `nonisolated` parce que leurs implémentations réelles sont des `actor` (isolation incompatible avec `@MainActor` par défaut, même mécanisme que le fix `durationInSeconds` en Leçon 22). `SongDownloading`/`LyricsFetching` sont implémentés par de simples `struct`, elles-mêmes implicitement `@MainActor` sous le même réglage — protocole et implémentation sont donc d'accord par défaut, d'où l'absence d'erreur de compilation sans `nonisolated`. Mais ça signifie que le Service réseau tourne aujourd'hui silencieusement sur le Main Actor sans que ce soit voulu — même principe que l'aside de la Leçon 7 (`@MainActor` protège l'UI, pas un Service qui parle au réseau). Pur déplacement, aucun changement de comportement. Nav de la Leçon 24 mise à jour pour pointer vers cette leçon. Comparée le 2026-07-30 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 25`) : structure en sous-dossiers `Domain/Interfaces/Repositories/` + `Domain/Interfaces/Services/` (pas prévu dans l'énoncé initial, intégré à la leçon) ; fichiers d'implémentation renommés d'après leur type concret (`SongDownloading.swift` → `MockSongDownloading.swift`, etc., cohérent). Écart substantiel repéré et challengé à la demande explicite de Nicolas ("challenge la cohérence des classes") : `SongDownloading`/`LyricsFetching` ont été renommés `SongDownloadingService`/`LyricsFetchingService`, sans toucher `LyricsRepository`/`DownloadedSongsRepository` — incohérence de nommage entre les deux paires. Vérifié via les Swift API Design Guidelines officielles (swift.org) : les protocoles "is-a" (Repository) doivent lire comme des noms, les protocoles de capacité doivent utiliser un suffixe `-able`/`-ible`/`-ing` — `SongDownloading`/`LyricsFetching` respectaient déjà cette règle avant le renommage ; `Service` en plus du `-ing` double le signal sans rien clarifier. Recommandation ajoutée dans la leçon : revenir à `SongDownloading`/`LyricsFetching` (renommage Xcode), garder `Services/` comme nom de dossier seulement. Le `nonisolated` manquant sur `LyricsFetching`(Service) n'a pas non plus été appliqué dans l'implémentation réelle — noté comme à faire dans le même passage que le renommage. Pas encore réappliqué par Nicolas au moment de cette note.
  - Suite (2026-07-30, même jour) : Nicolas a corrigé (renommage `SongDownloading`/`LyricsFetching` sans suffixe `Service`, `nonisolated` ajouté aux 4 protocoles — pas seulement `LyricsFetching`) et force-pushé par-dessus son propre commit `feat: implementation lecon 25` (`662fc2e` → `fe13c68`, même parent `4f70b6c` — aucune perte, juste son propre commit remplacé ; resynchronisé via `git stash` + `git reset --hard origin/main` + `git stash pop`, sans toucher aux fichiers de cette session). Rendre `LyricsFetching` nonisolated a cassé la compilation de `URLSessionLyricsFetching.swift`, corrigé avec 3 `await` (appel à `lyricsURL`, deux appels à `lyricsFetcherLogger.error` — fonction libre et constante globale restées `@MainActor` par défaut, même mécanisme que `durationInSeconds` en Leçon 22, révélé cette fois à 3 endroits) et `nonisolated private struct LyricsResponse: Decodable` (nécessaire spécifiquement parce que `JSONDecoder().decode(...)` est un appel synchrone — un `await` y est impossible, contrairement aux deux premiers cas). `SongDownloading`/`MockSongDownloading` n'a rien cassé, son corps n'appelant aucune déclaration implicitement `@MainActor`. Leçon mise à jour avec cette histoire complète (nouvelle section + aside), plutôt que le hedge initial.
    - Leçon 26 (écrite le 2026-07-30) — Deux bugs de concurrence réels, sans corrections publiées côté implémentation au moment de l'écriture : (1) Task orpheline dans `DownloadSongUseCase` — `repository.add(song)` appelé sans condition même après annulation ; corrigée par `let task = Task { ... }` + `continuation.onTermination = { _ in task.cancel() }` + `guard`/`if !Task.isCancelled`, code repris tel quel de l'audit (point #1). Insiste sur le fait que l'annulation d'une Task consommatrice ne se propage pas automatiquement à travers un `AsyncStream` (vérifié via Swift Forums, doc Apple sur `Continuation.onTermination` non chargeable directement — JS). (2) Task non trackée dans `SongDownloadViewModel.startDownload()` — même anti-pattern à l'autre bout du fil, sans quoi `onTermination` du bug #1 ne se déclenche jamais en pratique ; corrigée par `private var task` + `cancelDownload()` + `deinit { task?.cancel() }`, en citant directement `docs/swift6-isolation-bonnes-pratiques.md` (nouveau doc poussé par Nicolas le 2026-07-30, table anti-patterns) qui documente exactement ce cas. (3) `Song.id = UUID()` (audit point #2) — id régénéré à chaque instanciation, casse `storage.contains(song)` dans `SimpleDownloadedSongsRepository` et le cache `[Song.ID: [LyricsLine]]` de `CachedLyricsRepository` ; corrigée par `var id: String { "\(artist.lowercased())-\(title.lowercased())" }` (calculée, déterministe), reprise de l'audit. Note honnête ajoutée dans la leçon : ce bug est aujourd'hui *latent* avec `mockPlaylist` (constante globale évaluée une seule fois), ne se manifeste que si un `Song` déjà connu est reconstruit ailleurs (décodage futur, test, recherche) — pas une régression visible immédiatement testable en l'état actuel du mock. Nav de la Leçon 25 mise à jour pour pointer vers cette leçon.
  - Comparée le 2026-07-30 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 26`) : les 3 bugs corrigés conformément à ce qui était enseigné, avec 3 écarts identifiés et intégrés à la leçon. (1) `DownloadSongUseCase`/`Song.id` : conformes trait pour trait. (2) `SongDownloadViewModel` : nommage réel `downloadTask` (pas `task`), garde `!Task.isCancelled` ajoutée à la fois dans ET après la boucle `for await` (double sécurité, pas seulement après comme enseigné), `downloadTask = nil` après complétion (hygiène non enseignée). Surtout : pas de `deinit` — l'annulation est câblée sur `SongOptionsSheet.onDisappear { downloadViewModel?.cancelDownload() }` plutôt que sur la désallocation du ViewModel. Jugé et documenté comme un **meilleur choix** que le `deinit` initialement enseigné : le timing de désallocation d'un `@State` géré par SwiftUI n'est pas garanti immédiat (une vue peut persister en mémoire après avoir quitté l'écran, ex. pile de navigation), alors que `.onDisappear` est un signal explicite et déterministe du framework. (3) Découverte non anticipée : transformer `Song.id` de stockée (`let id = UUID()`) à calculée (`var id: String { ... }`) a cassé la compilation de `CachedLyricsRepository` (Leçon 25), qui indexe son cache par `Song.ID` — 2 `await` ajoutés devant `cache[song.id]`. Investigué via un sous-agent spécialiste Swift 6 (recherche groundée SE-0466/SE-0434, pas de réponse de mémoire) à la demande explicite de Nicolas ("aide-toi d'un spécialiste Swift 6 pour écrire tes leçons") : la cause précise n'est pas `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` seul (SE-0466, qui isole tout par défaut, stocké et calculé confondus) mais une proposition distincte, **SE-0434**, qui exempte spécifiquement les propriétés *stockées* de type `Sendable` (inférées `nonisolated` dans le module qui les définit) — exemption qui ne couvre pas les propriétés *calculées*, dont le corps exécute du code que le compilateur ne peut garantir inoffensif. `let id = UUID()` bénéficiait de cette exemption (stocké) ; `var id: String { ... }` non (calculé) — même mécanisme que `durationInSeconds` (Leçon 22), preuve visible dans le même fichier (une ligne plus loin, `song.durationInSeconds`, déjà `nonisolated`, s'utilise sans `await` juste à côté de `song.id` qui en réclame un). Nicolas a corrigé avec `await` aux deux sites d'appel (fonctionnellement correct) ; la leçon documente aussi la correction alternative recommandée par cohérence avec `durationInSeconds` — `nonisolated var id: String { ... }`, non appliquée par Nicolas au moment de cette note, juste suggérée. Sous-agent a aussi confirmé (doc Apple + source stdlib) que l'annulation d'une Task consommatrice déclenche bien `onTermination` avec le cas `.cancelled`, distinct de `.finished` — utilisé pour sourcer la leçon plus précisément qu'un simple lien Swift Forums. Leçon mise à jour (nouvelle section H2, table, quiz 7-8, source-box étoffée) et poussée.
- **Consigne permanente ajoutée le 2026-07-30, à la demande explicite de Nicolas** : pour les prochaines leçons (Leçon 27 et suivantes), consulter un sous-agent spécialiste Swift 6/Concurrency (research groundé via WebSearch/WebFetch, pas de réponse de mémoire) pour vérifier les affirmations techniques non triviales avant de les écrire dans une leçon — plutôt que de les rédiger directement à partir de connaissances parametriques. Déclenché après que la Leçon 26 ait nécessité plusieurs corrections post-implémentation sur des points de mécanique Swift 6 fine (isolation des computed properties). À appliquer systématiquement, pas seulement quand un doute apparaît après coup.
    - Leçon 27 (écrite le 2026-07-30, pas encore implémentée par Nicolas) — Le service de téléchargement devient réel : (1) `SongDownloading` enregistré comme Factory séparée dans `KaramockContainer` (`self.songDownloading()`) plutôt que construit en dur dans `downloadSong` (audit #5) — comportement de prod inchangé, dépendance enfin nommée/remplaçable. (2) `SongDownloading.download()`/`DownloadSongUseCase.callAsFunction` passent de `AsyncStream<Double>` à `AsyncThrowingStream<Double, Error>`, rendant `DownloadState.failed` enfin atteignable ; `SongDownloadViewModel.startDownload()` gagne un `do/catch` qui bascule sur `.failed`. (3) `MockSongDownloading` : `try?` (avalait `CancellationError`) remplacé par un `do/catch` explicite qui distingue annulation propre (`catch is CancellationError` → `continuation.finish()`) d'une vraie erreur (`catch` générique → `continuation.finish(throwing:)`) ; ajout d'un `shouldFail: Bool = false` (comportement de prod inchangé par défaut) pour pouvoir déclencher et vérifier concrètement le chemin `.failed`, sinon jamais exercé par le mock. Deux points groundés via sous-agent spécialiste (pas de mémoire) avant rédaction, appliquant la nouvelle consigne ci-dessus : (a) `AsyncThrowingStream<Element, Failure>` n'accepte `Failure` concret dans aucun initialiseur public — seul `Failure == Error` compile (limitation stdlib actuelle, cf. swiftlang/swift#75853 toujours ouvert), donc pas de typed throws bout-en-bout possible ici contrairement à `fetchLyrics`/`LyricsError` ; (b) l'annulation d'un `AsyncThrowingStream` ne lève PAS d'erreur dans un `catch` englobant — elle termine silencieusement l'itération (confirmé par Franz Busch, équipe Swift, sur Swift Forums) — d'où le besoin de garder les `guard !Task.isCancelled` de la Leçon 26 même après le passage au throwing. Sendability vérifiée aussi : `AsyncThrowingStream<Double, Error>` est `@unchecked Sendable` dès que `Element` l'est (pas de contrainte sur `Failure`), parce que `Error` hérite lui-même de `Sendable` dans sa déclaration stdlib. Scope explicite : ne construit pas de vraie implémentation réseau (`URLSessionSongDownloading` suggéré par l'audit) — pas de backend de téléchargement réel dans ce projet, "devient réel" = plomberie de production (déclaré, throwing, annulation propre), pas un faux réseau inventé. Nav de la Leçon 26 mise à jour pour pointer vers cette leçon. Comparée le 2026-07-30 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 27`) : conforme trait pour trait à ce qui était enseigné — `songDownloading: Factory<SongDownloading>` dans le container, `DownloadSongUseCase`/`SongDownloadViewModel`/`MockSongDownloading` identiques au code de la leçon (mêmes noms, même `shouldFail`/`SimulatedDownloadFailure`, même structure `do/catch is CancellationError`) — aucune correction nécessaire, deuxième leçon sans écart après la Leçon 24.
    - Leçon 27 (à venir) — Le service de téléchargement devient réel : enregistrer `SongDownloading` dans le Container (`MockSongDownloading` actuellement câblé en dur en prod), le rendre throwing pour que `.failed` soit atteignable, corriger le `try?` qui avale `CancellationError` dans le mock.
    - Leçon 28 (écrite le 2026-07-30, pas encore implémentée) — Repository range, Mapper transforme : (1) `LyricsMapper` (`Domain/Mappers/LyricsMapper.swift`, `nonisolated enum` avec `static func map(rawText:duration:) -> [LyricsLine]`) extrait de `CachedLyricsRepository` (SRP, audit #8) — `nonisolated` nécessaire malgré la pureté de la fonction, groundé via sous-agent spécialiste (SE-0466 n'exempte rien sur la base de la pureté, mécanisme purement syntaxique/par déclaration). (2) `lyricsURL` encapsulée comme méthode privée de `URLSessionLyricsFetching` (audit 2.10), qui devient `nonisolated struct` (SE-0449, Swift 6.1 — nonisolated applicable à un type entier, propage à tous ses membres) — simplifie le correctif de la Leçon 25 en retirant le besoin d'annoter `lyricsURL` individuellement. Point vérifié via sous-agent avant rédaction : `fetchLyrics` compilait déjà sans `nonisolated` explicite car elle hérite de l'isolation par inférence de conformance au protocole `nonisolated protocol LyricsFetching` (exemption listée explicitement par SE-0466) — mais cette inférence ne s'étend PAS aux méthodes voisines non-requirements comme `lyricsURL`, chacune reste indépendamment @MainActor par défaut sauf annotation explicite (ou annotation du type entier). Point important resté inchangé et documenté : `lyricsFetcherLogger` (constante globale hors du type) n'est PAS couverte par `nonisolated struct` — les 2 `await lyricsFetcherLogger.error(...)` de la Leçon 25 restent nécessaires, volontairement hors périmètre de cette leçon (seul `lyricsURL` était demandé par l'audit). (3) `SimpleDownloadedSongsRepository` → `InMemoryDownloadedSongsRepository` (audit 1.7, renommage pur, aucun changement de comportement). (4) Écart assumé et explicité avec l'audit (point #9, "déplacer mockLyrics/MockSongDownloading vers KaramockTests/Mocks/") : vérifié que les deux sont en réalité utilisés en production (`mockLyrics` = état initial + fallback dans `FullScreenPlayerViewModel`/`FullScreenPlayerView` ; `MockSongDownloading` = LA seule implémentation de `SongDownloading` enregistrée dans le container depuis la Leçon 27, faute de vrai backend) — les déplacer purement vers la cible de tests casserait la compilation de l'app. Recommandation de la leçon, différente de l'audit littéral mais fidèle à son intention : renommer plutôt que déplacer (`mockLyrics` → `placeholderLyrics`, `MockSongDownloading` → `SimulatedSongDownloading`, `shouldFail` retiré de la version prod) ; le nom `MockSongDownloading` est ensuite réutilisé pour un VRAI mock, nouveau, dans `KaramockTests/Services/MockSongDownloading.swift` (miroir de la convention déjà en place avec `KaramockTests/Repositories/MockDownloadedSongsRepository.swift`), qui garde `shouldFail` pour la Leçon 30. Explicitement présenté dans la leçon comme "l'audit est un instantané, pas une spécification figée — les Leçons 26/27 ont changé un fait sur lequel sa recommandation reposait." Nav de la Leçon 27 mise à jour pour pointer vers cette leçon. Comparée le 2026-07-31 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 28`) : conforme trait pour trait à ce qui était enseigné — `LyricsMapper` (`nonisolated enum`), `CachedLyricsRepository` simplifié, `InMemoryDownloadedSongsRepository`, `SimulatedSongDownloading` en prod (sans `shouldFail`), `URLSessionLyricsFetching` en `nonisolated struct` avec `lyricsURL` encapsulée en méthode privée (les 2 `await lyricsFetcherLogger.error(...)` bien laissés inchangés, comme anticipé dans l'aside dédié), `placeholderLyrics` (2 sites d'appel mis à jour), `MockSongDownloading` déplacé vers `KaramockTests/Services/MockSongDownloading.swift` avec `@testable import Karamock` et `shouldFail` conservé. Seul écart cosmétique, sans impact : la signature du mock de test qualifie `Karamock.Song` plutôt que `Song` — aucune correction nécessaire. Troisième leçon sans écart après les Leçons 24 et 27.
    - Leçon 29 (écrite le 2026-07-31, pas encore implémentée) — `SongOptionsSheet` gagne son ViewModel. Écart assumé avec l'audit dès l'ouverture de la leçon : l'audit traite `DiscoveryView` et `SongOptionsSheet` avec la même sévérité ("Views manquant un ViewModel"), mais `DiscoveryView` n'a aujourd'hui aucun état/logique à extraire (juste `PlaylistSection(playlists: mockPlaylist)`) — créer un `DiscoveryViewModel` maintenant serait une abstraction sans travail réel à faire, explicitement refusée dans la leçon (même famille de raisonnement que l'écart avec l'audit en Leçon 28). Seul `SongOptionsSheet` est traité : nouveau `SongOptionsViewModel` (`@MainActor @Observable`, `song`/`player`/`downloadViewModel` injectés au constructeur) qui absorbe les 5 `@State` (`mode`, `singerName`, `adjustVolumes`, `pitch`, `tempo`) et la mutation directe de `PlayerState` ("Jouer maintenant" → `playNow()`) ; `dismiss()` reste dans la View (mécanisme SwiftUI, pas de logique métier). `downloadViewModel` composé DANS la nouvelle Factory `songOptionsViewModel` (`self.songDownloadViewModel(song)`) plutôt que récupéré séparément — la View garde un seul appel à `Container.shared` dans `.task`, comme avant (limite déjà actée Leçon 12/22, pas résolue ni cherchée à résoudre ici). Nouvelle sous-vue `SongOptionsForm` (fichier séparé) avec `@Bindable var viewModel: SongOptionsViewModel` en **propriété stockée** plutôt qu'en variable locale de `body` — point vérifié via sous-agent spécialiste avant rédaction : `@Bindable` refuse un type `Optional<T>` même quand `T: Observable` (confirmé, erreur de compilation "The wrapped value must be an object that conforms to Observable", donc `@Bindable var viewModel = viewModel` sur le `SongOptionsViewModel?` de la Sheet ne peut pas marcher tel quel) ; la contrainte "@Bindable doit être la première ligne de body" observée empiriquement en Leçon 10 avec `@Injected` n'a par ailleurs AUCUNE confirmation officielle trouvée (ni Swift Evolution, ni doc Apple, ni bug tracker) — présentée dans la Leçon 29 comme observation empirique limitée, pas comme règle générale, avec l'extraction de sous-vue proposée comme façon de l'éviter complètement plutôt que d'en débattre. Nav de la Leçon 28 mise à jour pour pointer vers cette leçon. Comparée le 2026-07-31 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 29`) : structure globale conforme (`SongOptionsViewModel`, `songOptionsViewModel` Factory composant `self.songDownloadViewModel(song)`, `SongOptionsForm` avec `@Bindable`), deux écarts trouvés et traités différemment. (1) **Erreur repérée dans mon propre énoncé, corrigée** : j'avais écrit `private struct SongOptionsForm` dans son propre fichier, alors qu'il est utilisé depuis `SongOptionsSheet.swift` — un fichier différent. `private` sur une déclaration de niveau fichier restreint l'accès au fichier qui la déclare (équivalent à `fileprivate` pour le niveau top-level) ; ça n'aurait pas compilé. Nicolas a implémenté sans `private` (correctement) — leçon corrigée pour retirer le `private` erroné, avec une étape de vérification et une question de quiz (Q7) ajoutées sur ce point précis d'access control. (2) **Écart challengé, non corrigé dans l'implémentation réelle** : `SongOptionsViewModel.downloadViewModel` a été déclaré `var downloadViewModel: SongDownloadViewModel?` avec `init(..., downloadViewModel: SongDownloadViewModel? = nil)`, alors que la seule Factory de production (`songOptionsViewModel`) fournit toujours une vraie instance — l'optionnalité ne sert qu'à simplifier la construction en test, jamais nil en usage réel. Même catégorie de problème que `shouldFail` en Leçon 28 (commodité de test infiltrée dans un type de production). Nouvelle section ajoutée dans la leçon ("Un optionnel qui n'a pas de raison d'être") recommandant `let downloadViewModel: SongDownloadViewModel` non optionnel, sans valeur par défaut — pas encore appliqué par Nicolas au moment de cette note, juste recommandé et documenté (nouvelle question de quiz Q8).
    - Leçon 30 (écrite le 2026-07-31, pas encore implémentée) — Preuve par test que les bugs de la Leçon 26 ne reviendront pas, ferme le cycle Nettoyage architecture (Leçons 24 à 30). Trois tests Swift Testing, groundés via sous-agent spécialiste avant rédaction (nouvelle consigne permanente du 2026-07-30) : (1) `SongIdentityTests` — deux `Song` construits séparément avec même artiste/titre ont le même `.id` (Bug #3). Seul test des trois n'utilisant ni `@Suite(.container)` ni Factory (aucune dépendance à substituer), mais nécessitant quand même `@MainActor` : `id` est une propriété *calculée*, non exemptée par SE-0434, donc reste implicitement isolée au Main Actor sous SE-0466 puisque Nicolas n'a pas (encore) appliqué le `nonisolated var id` recommandé en Leçon 26. Point subtil ajouté après vérification via SE-0185 (Synthesizing Equatable/Hashable) : le `Hashable`/`Equatable` synthétisé de `Song` ignore désormais totalement `id` puisqu'il est devenu calculé (synthèse = uniquement les propriétés stockées) — `first == second` est donc vrai pour une raison plus profonde que la correction affichée de la Leçon 26 (avant le fix, `id` stocké faisait partie de la comparaison synthétisée, c'était exactement le piège documenté en Leçon 13 ; après le fix, `id` n'entre même plus en ligne de compte dans `==`). (2) `SongDownloadCancellationTests` — annuler un téléchargement en cours (`cancelDownload()`) ne doit jamais faire persister la chanson (Bugs #1+#2 ensemble), testé en pilotant `SongDownloadViewModel` de bout en bout via `Container.shared.songDownloadViewModel(song)` (même chaîne qu'en production). Repose sur une attente bornée assumée (250 ms puis annulation, puis 2500 ms — plus long que les 10x200ms du mock) : vérifié via sous-agent spécialiste que `confirmation(expectedCount: 0)` de Swift Testing NE résout PAS ce problème (elle ne vérifie que les appels reçus pendant sa propre closure, sans attendre d'événement asynchrone ultérieur — confirmé par l'issue swift-testing #978, fermée "not planned", qui décrit exactement ce scénario) ; aucune alternative pleinement déterministe n'existe aujourd'hui tant que la Task interne de `DownloadSongUseCase`/`downloadTask` du ViewModel ne remonte aucun signal testable de fin de nettoyage — attente bornée présentée comme limite honnête de l'écosystème, pas comme négligence. Précision aussi apportée sur `onTermination` (vérifié dans le code source stdlib d'`AsyncThrowingStream`) : le déclencheur documenté est l'annulation de la Task *consommatrice*, pas la simple sortie d'un `for await` par `break`/`return` — la chaîne réelle est `cancelDownload()` → annule `downloadTask` (consommateur du stream de `DownloadSongUseCase`) → déclenche `onTermination` de CE stream → annule la Task interne du UseCase. (3) `SongDownloadFailureTests` — un téléchargement en échec bascule vers `.failed`, promesse explicite de la Leçon 28 ("la bonne façon de vérifier .failed devient un vrai test, exactement le sujet de la Leçon 30"), utilise `MockSongDownloading(shouldFail: true)` (le vrai mock de test créé en Leçon 28, distinct de `SimulatedSongDownloading` la Factory de prod qui ne peut jamais échouer) avec une attente bornée plus courte et plus sûre (1250 ms, le mock échoue à l'étape 5 = 1000 ms) car c'est un événement positif attendu à un instant connu, pas une absence à prouver indéfiniment. Nav de la Leçon 29 mise à jour pour pointer vers cette leçon. Comparée le 2026-07-31 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 30`) : conforme trait pour trait à ce qui était enseigné — les trois fichiers de tests reproduisent exactement les mêmes overrides de Container, les mêmes durées d'attente bornée (250ms/2500ms pour l'annulation, 1250ms pour l'échec) et les mêmes assertions. Seuls écarts, purement cosmétiques, sans impact : les trois fichiers vivent à plat dans `KaramockTests/` plutôt que dans des sous-dossiers `Domain/`/`ViewModels/` (simples commentaires d'en-tête dans la leçon, jamais une exigence) ; données de chanson factices différentes (`"Kasmir"`/`"A title"` plutôt que `"Kashmir"`) ; une fonction nommée au singulier (`songWithSameArtistAndTitleShareTheSameIdentity`) plutôt qu'au pluriel enseigné. Quatrième leçon sans écart fonctionnel après les Leçons 24, 27 et 28 — arc "Nettoyage architecture" (Leçons 24 à 30) désormais clos.
  - Programme reformulé une nouvelle fois (décidé 2026-07-31, à la demande de Nicolas) — deux nouveaux axes majeurs, dans cet ordre confirmé via question posée à Nicolas :
    - Axe A — tvOS :
      - Leçon 31 (écrite le 2026-07-31, revue et corrigée le 2026-08-02 après comparaison avec l'implémentation réelle) — Ajouter tvOS via *Supported Destinations* (Xcode 14+, General → Supported Destinations → +), pas un second target ni le template Multiplatform App — vérifié via la doc Apple officielle ("Configuring a multiplatform app target") + notes de version Xcode 14, qui confirment explicitement que ça marche en partant d'un target mono-plateforme déjà existant. Cinq erreurs de compilation réelles, dans cinq fichiers, groundées via sous-agent spécialiste (recherche WebFetch/WebSearch sur la doc Apple, pas de mémoire) : (1) `.tabViewBottomAccessory` (`RootTabView.swift`) — disponible uniquement iOS/iPadOS/Mac Catalyst 26.1, aucune entrée tvOS à aucune version. (2) `.navigationBarTitleDisplayMode(.inline)` (`PlaylistDetailView.swift`) — absent de tvOS et même de macOS. (3) `Color(.secondarySystemBackground)` (`ModeCard.swift`) — précision importante : le coupable est `UIColor.secondarySystemBackground` (absent tvOS), pas `Color(uiColor:)` lui-même (disponible tvOS 15.0+). (4) `.textFieldStyle(.roundedBorder)` (`SongOptionsForm.swift`) — absent de tvOS, et découverte annexe : déprécié depuis 27.0 même sur les plateformes qui le supportent. (5) `Slider` — jamais disponible sur tvOS à aucune version, utilisé 3 fois dans 2 fichiers (`SongOptionsForm.swift` : tonalité/tempo ; `FullScreenPlayerView.swift` : barre de progression) ; `Stepper`, le remplacement "évident", n'est pas non plus disponible sur tvOS (aucun contrôle natif SwiftUI de saisie par plage, un vrai vide comblé seulement par des bibliothèques tierces comme `zattoo/TvOSSlider`). Toutes corrigées par `#if os(iOS)` autour de l'appel, sans repli visuel de remplacement (pas de `Text` "indisponible" — juste l'absence du contrôle). Fait surprenant documenté en aside : `.tabBarMinimizeBehavior(.onScrollDown)`, juste à côté de `.tabViewBottomAccessory`, compile SANS erreur sur tvOS (disponible tvOS 26.0 d'après sa doc) malgré son lien conceptuel avec une barre d'onglets en bas d'écran absente sur tvOS — effet réel non documenté par Apple, laissé en vérification visuelle dans le scénario de test plutôt qu'affirmé. `FactoryKit` vérifié sans changement nécessaire (`Package.swift` déclare `.tvOS(.v13)` explicitement) ; `@Observable`/`@Bindable`/`NavigationStack`/`sheet`/`fullScreenCover`/`ScrollViewReader`/`@ScaledMetric` tous déjà compatibles tvOS 13-17+, bien en dessous du plancher tvOS impliqué par l'ajout de la destination. Nav de la Leçon 30 mise à jour pour pointer vers cette leçon. Comparée le 2026-08-02 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 31`) : écart significatif trouvé et corrigé. La version initiale de la leçon n'annonçait que **deux** erreurs de compilation (`.tabViewBottomAccessory` et `Slider` x2) ; l'implémentation réelle touchait en fait **cinq fichiers** — `PlaylistDetailView.swift` (`.navigationBarTitleDisplayMode`), `ModeCard.swift` (`Color(.secondarySystemBackground)`) et un troisième `Slider` dans `FullScreenPlayerView.swift` n'avaient pas été anticipés. Les trois faits manquants ont été vérifiés via sous-agent spécialiste (doc Apple officielle pour chacun) avant correction de la leçon plutôt que devinés. Deuxième écart : la leçon initiale proposait un repli `Text("... indisponible sur tvOS")` pour tonalité/tempo ; l'implémentation réelle n'ajoute aucun repli, elle masque simplement le `Slider` (la valeur numérique déjà affichée au-dessus reste la seule indication) — leçon corrigée pour refléter ce choix plus minimal. Troisième écart, cosmétique celui-là : l'implémentation réelle regroupe `.tabBarMinimizeBehavior` et `.tabViewBottomAccessory` sous le même `#if os(iOS)`, alors que la leçon initiale les séparait (le premier compile seul sur tvOS) — gardé tel quel dans la leçon corrigée avec une note explicite que ce regroupement est un choix de simplicité, pas une nécessité technique, pour ne pas contredire le fait documenté juste au-dessus. Deux nouvelles questions de quiz ajoutées (Q9 sur la nuance Color/UIColor, Q10 sur le compte total des fichiers touchés). Cinquième leçon avec écart d'implémentation (après la Leçon 29), mais la seule où l'écart porte sur l'exhaustivité du diagnostic plutôt que sur un détail de code.
      - Leçon 32 (écrite le 2026-08-02, revue après implémentation le même jour) — Pour chacun des 5 éléments restreints à iOS en Leçon 31 (`#if os(iOS)`), une vraie réponse tvOS demandée explicitement par Nicolas, quitte à créer des composants dédiés. Tous les faits groundés via deux sous-agents spécialistes en parallèle (recherche doc Apple officielle) avant rédaction. Traité du plus simple au plus complexe : (1) `navigationBarTitleDisplayMode` — aucun équivalent tvOS trouvé, rien ajouté, absence documentée plutôt que devinée. (2) `.textFieldStyle(.roundedBorder)` → `.automatic` explicite (tvOS 13+, déjà le défaut par omission, mais rendu explicite pour documenter le choix). (3) `Color(.secondarySystemBackground)` → `.regularMaterial` (confirmé tvOS 15+) + bordure violette conditionnelle en overlay, car `Material` ne prend pas d'opacité comme `Color` et ne peut donc pas seul distinguer l'état sélectionné. (4) `Slider` (tonalité/tempo/progression, 3 sites) → nouveau composant partagé `FocusStepperTV` (`Views/Common/`), deux `Button` +/- : vérifié que `Button` gère seul le focus et l'activation télécommande sur tvOS (doc officielle citée explicitement), donc aucun `.focusable()`/`@FocusState` nécessaire ; `CardButtonStyle` (`.card`, tvOS 14+ exclusif) écarté car pensé pour des cartes de contenu à parcourir, pas un bouton icône — `.plain` retenu (déjà la convention du projet dans `MiniPlayerBar.swift`) ; aucun mécanisme de presser-maintenir/accélération trouvé dans la doc officielle, donc volontairement absent (clic simple = un pas), documenté en aside plutôt qu'inventé. (5) `.tabViewBottomAccessory` (mini-lecteur) → nouveau composant dédié `MiniPlayerBarTV` (`Views/Player/MiniPlayerBar/`), branché via `.overlay(alignment: .bottom)` faute d'API tvOS équivalente ; volontairement un seul `Button` (ouvrir le lecteur plein écran) plutôt que la structure iOS (`onTapGesture` + `Button` imbriqué pour play/pause) — le Focus Engine ne peut donner le focus qu'à un seul élément activable à la fois, donc un Button imbriqué dans un autre n'est pas fiable ; le vrai bouton play/pause reste exclusivement dans `FullScreenPlayerView`. Nav de la Leçon 31 mise à jour pour pointer vers cette leçon. Comparée le 2026-08-02 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 32`) : conforme quasiment trait pour trait à ce qui était enseigné, y compris `PlaylistDetailView.swift` laissé intact (confirme que "aucune alternative" était bien la bonne réponse) et `FocusStepperTV`/`MiniPlayerBarTV` repris mot pour mot. Un seul écart trouvé, ajouté en aside dans la leçon plutôt que corrigé silencieusement : le stepper du tempo utilise un pas de 1 (`viewModel.tempo - 1`/`+ 1`) au lieu du pas de 5 enseigné (cohérent avec le `Slider(..., step: 5)` d'origine sur iOS) — vraisemblablement un copier-coller du bloc tonalité sans ajuster le pas. Signalé à Nicolas, pas corrigé dans le code ni dans l'exemple enseigné (qui restait correct). Sixième leçon avec écart d'implémentation (après les Leçons 29 et 31), la première où l'écart est aussi mineur et isolé à un seul paramètre.
      - Leçon 33 (écrite le 2026-08-02, revue après implémentation le même jour) — Corrige un vrai bug visuel signalé par Nicolas : `FullScreenPlayerView`, présenté en `.fullScreenCover` depuis la Leçon 23, s'affiche sans fond sur tvOS et se dessine par-dessus les onglets de l'app (contrairement à iOS où la modale couvre proprement l'écran). Enquête menée via quatre sous-agents spécialistes en parallèle/séquence avant tout correctif : (1) aucune cause tvOS-spécifique documentée par Apple — HIG "Modality" et "Sheets" disent chacune mot pour mot "No additional considerations for tvOS" ; `fullScreenCover` listé tvOS 14.0+ sans note de comportement particulier. (2) Option testée et explicitement écartée : `.presentationBackground(_:)` (tvOS 16.4+, confirmé) ou `.background(_:)` (motif de l'exemple officiel Apple sur la doc `fullScreenCover`) résoudraient probablement le symptôme, mais patchent l'opacité sans changer le mécanisme. (3) Correctif adopté : `NavigationStack` + `.navigationDestination(isPresented:)` (tvOS 16.0+, confirmé) autour du `TabView` dans `RootTabView.swift`, remplaçant le `.fullScreenCover` sur tvOS uniquement (iOS inchangé) — une vue poussée dans une pile remplit son conteneur par construction, éliminant toute la classe de bug plutôt que de patcher un symptôme ; cohérent aussi avec le vocabulaire "parent screen" de la page HIG tvOS "Remotes" pour le bouton Back. Fait vérifié et assumé comme décision d'ingénierie plutôt que prescription Apple : imbriquer une `NavigationStack` autour d'un `TabView` dont chaque onglet (`DiscoveryView`, `LibraryView`) a déjà sa propre `NavigationStack` n'est ni recommandé ni déconseillé par la doc officielle — ce cas précis n'y est simplement pas traité. `TabView` extrait en propriété calculée `tabs` partagée entre les deux branches `#if os(iOS)/#else` pour éviter la duplication. Aucune modification nécessaire dans `FullScreenPlayerView.swift`, `SongOptionsViewModel.swift` ni `MiniPlayerBarTV.swift` : `dismiss()` via `@Environment(\.dismiss)` fonctionne génériquement pour fermer une modale ou dépiler une destination, et `SongOptionsSheet` appelle déjà `dismiss()` explicitement sur son bouton "Jouer maintenant" indépendamment de `player.isExpanded`. Écart assumé avec la recommandation générale d'Apple ("favor binding a path to a navigation stack for programmatic navigation") : la version booléenne `isPresented:` est gardée plutôt qu'un `NavigationPath` dédié, car `player.isExpanded` est déjà un `Bool` partagé existant dans `PlayerState` — introduire un type de destination juste pour ce seul cas ajouterait de la complexité sans bénéfice. Deux points explicitement laissés non confirmés, à vérifier sur le vrai matériel : le bouton Menu de la télécommande dépile-t-il automatiquement la destination poussée (fortement suggéré par l'existence d'`isModalInPresentation` sur tvOS, jamais affirmé noir sur blanc par Apple) ; sinon `.onExitCommand(perform:)` (tvOS 13+/macOS uniquement) reste le filet de secours documenté. Détail cosmétique noté mais explicitement hors périmètre : l'icône `chevron.down` du bouton de fermeture, pensée pour une métaphore de glissement tactile absente sur tvOS. Nav de la Leçon 32 mise à jour pour pointer vers cette leçon. Comparée le 2026-08-02 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 33`) : conforme trait pour trait à ce qui était enseigné — `tabs` extrait en propriété calculée, branches `#if os(iOS)` (`.fullScreenCover`) / `#else` (`NavigationStack` + `.navigationDestination(isPresented:)`) identiques, et seul `RootTabView.swift` touché comme anticipé (`FullScreenPlayerView.swift`, `SongOptionsViewModel.swift`, `MiniPlayerBarTV.swift` inchangés). Aucun écart, aucune correction nécessaire. Septième leçon comparée à une implémentation réelle, deuxième sans le moindre écart après la Leçon 30.
      - Leçon 34 (écrite le 2026-08-02, revue après implémentation partielle le 2026-08-03) — Réévaluation critique de `audits/2026-08-02-tvos-audit.md` (audit externe, 3 agents Copilot, note globale 4.2/10, 22 problèmes listés) : chaque remarque vérifiée contre la doc Apple officielle avant tout correctif, via cinq sous-agents spécialistes au total sur ce fil. Deux des six problèmes "critiques" de l'audit sont factuellement faux : C4 (`MiniPlayerBar.swift` "compile error, API iOS 18+") — `tabViewBottomAccessoryPlacement` est en réalité tvOS 26.0+, disponible sur les deux plateformes à la même version ; C5 (`.borderedProminent`/`.bordered` "n'existent pas sur tvOS") — confirmés tvOS 15.0+ et 13.0+. Un troisième (C2, `NavigationStack` imbriqués = "comportement indéfini, plante") dramatise une observation structurelle correcte mais non documentée comme dangereuse par Apple — et c'est la structure qui tourne sans incident depuis la Leçon 33. Un quatrième (C1, `.sheet()` "ignoré silencieusement sur tvOS") contredit la page HIG "Sheets" elle-même ("a sheet is always modal" sur tvOS, "No additional considerations"). I1/M1/M5 (`.buttonStyle(.plain)` "supprime le halo de focus") réfutés par la doc `PlainButtonStyle` elle-même ("may apply a visual effect to indicate... the focused... state"). I3 (revenir à `fullScreenCover` + `.persistentSystemOverlays(.hidden)` pour le lecteur) rejeté explicitement : annulerait probablement le correctif de la Leçon 33, et `persistentSystemOverlays` ne documente aucun effet pertinent sur tvOS (concepts iOS/iPadOS/visionOS — home indicator, SharePlay — absents de tvOS). C6/I9 (boutons précédent/suivant vides, `PlayerState` à enrichir) confirmés réels mais explicitement déclinés : pas un bug tvOS (identique sur iOS), et la solution est un chantier produit hors périmètre de l'axe tvOS. Corrections effectivement adoptées, toutes vérifiées : (1) `.onPlayPauseCommand()` (tvOS 13+ exclusif) sur `FullScreenPlayerView` et `MiniPlayerBarTV` — synergie notée avec la Leçon 32 : commande télécommande globale, pas un second `Button` focusable, donc compatible avec la règle "un seul élément focusable par composant" qui avait empêché un bouton play/pause dédié sur `MiniPlayerBarTV`. (2) `.focusSection()` (tvOS 15+) sur l'overlay `MiniPlayerBarTV` dans `RootTabView`. (3) Labels de temps figés ("0:00" en dur) corrigés avec le helper `formatTime(_:)` déjà présent dans `Views/Common/TimeFormatter.swift` — bug réel mais pas tvOS-spécifique, corrigé au passage. (4) Labels d'accessibilité de `FocusStepperTV` paramétrés (`decrementLabel`/`incrementLabel`, défauts "Diminuer"/"Augmenter") plutôt que le correctif de l'audit (M6, "Reculer de 10s"/"Avancer de 10s" codé en dur) — écart assumé et corrigé : `FocusStepperTV` est partagé entre 3 contextes (tonalité, tempo, progression), un label pensé pour la progression uniquement serait faux pour les deux autres. (5) Taille des boutons `FocusStepperTV` augmentée via la police (`.system(size: 40)` sur tvOS) plutôt que le `frame(width: 80, height: 80)` suggéré par l'audit — écart assumé : ce sont des icônes SF Symbols sans fond, un frame invisible ajoute de l'espace vide plutôt que d'agrandir ce qui est visible. (6) Tailles/espacements pour écran 10 pieds (`PlaylistCard` 150→280, `SongRow` artwork 50→80 + padding 4→16, `SongHeader` artwork 60→100 + police caption→callout, `PlaylistCoverHeader` 220→400, `SongOptionsForm` spacing 12→24) — tous vérifiés exacts dans le code avant correctif. (7) `StretchyView` désactivée sur tvOS (`#if os(iOS)`, effet de scroll continu non pertinent pour la télécommande). (8) `.refreshable` retiré sur tvOS dans `LibraryView` — confirmé tvOS 15+ (ne casse rien), mais son geste "pull-to-refresh" n'est documenté que pour iOS/iPadOS : retiré comme code mort plutôt que comme correctif de crash. Un correctif tenté sans garantie, documenté comme tel : `.focusable()` sur le `ScrollView` de `LyricsView` (I2) — aucune doc officielle ne confirme qu'il active le défilement par swipe sur tvOS, seulement qu'il fait de la vue une cible de focus ; appliqué à titre d'essai, à vérifier empiriquement par Nicolas. Nav de la Leçon 33 mise à jour pour pointer vers cette leçon. Comparée le 2026-08-03 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 34`) : 7 des 10 correctifs appliqués mot pour mot (`.onPlayPauseCommand` x2, `.focusSection()`, labels de temps, tailles `PlaylistCard`/`SongRow`/`SongHeader`, spacing `SongOptionsForm`, `StretchyView`, `.refreshable`). Trois omis, par simple incomplétude et non par désaccord — le code enseigné reste correct, juste pas encore recopié : les labels d'accessibilité paramétrés et l'agrandissement des boutons de `FocusStepperTV` (fichier non touché par le commit), la hauteur tvOS de `PlaylistCoverHeader` (toujours 220pt sur les deux plateformes), et `.focusable()` sur `LyricsView` (fichier non touché). Note ajoutée en tête de la section "Corrections adoptées" de la leçon pour refléter cet état sans réécrire le contenu, qui reste correct. Huitième leçon comparée à une implémentation réelle, la première avec des omissions plutôt que des erreurs. **Correction rétroactive (2026-08-03, à la vérification de la Leçon 35)** : le code `.onPlayPauseCommand` enseigné en section 1 (`FullScreenPlayerView.swift`/`MiniPlayerBarTV.swift`) manquait bien une garde `#if os(tvOS)` — `.onPlayPauseCommand` est une API tvOS 13+ exclusive (déjà documenté ainsi dans la leçon elle-même), et ces deux fichiers compilent pour iOS et tvOS depuis la même target (mécanisme de la Leçon 31). Sans garde, la destination iOS ne compile pas — bug resté invisible tant que seule la destination tvOS était buildée. Repéré et corrigé par Nicolas dans le commit `feat: implementation lecon 35`, à l'étape de son scénario de vérification qui impose un build croisé iOS + tvOS. Code de la leçon et sa note de statut corrigés en conséquence.
    - Axe A (tvOS) clos le 2026-08-03, à la demande de Nicolas : les trois correctifs restants de la Leçon 34 (labels/taille des boutons `FocusStepperTV`, hauteur tvOS de `PlaylistCoverHeader`, `.focusable()` sur `LyricsView`) sont explicitement **mis de côté pour l'instant**, pas abandonnés — jugés non prioritaires plutôt qu'incorrects. Ne pas les replanifier sans demande explicite de Nicolas, même logique que le backlog de la Leçon 26 ci-dessous.
    - Axe B — Moteur de paroles en C++ (démarré le 2026-08-03) : mini moteur karaoké en C++, intégré via l'interop Swift/C++, qui remplace la logique SwiftUI actuelle de gestion des paroles. Scope confirmé : garde le même modèle de timing simulé qu'aujourd'hui (répartition uniforme sur la durée) — objectif pédagogique = l'interop Swift/C++, pas un vrai moteur audio karaoké. Cohérent avec l'exclusion "vrai moteur audio" de `MISSION.md`, inchangée.
      - Leçon 35 (écrite le 2026-08-03, implémentée et vérifiée le 2026-08-03) — Premier jalon de l'axe B : le mécanisme d'interop Swift/C++, pas encore le portage de `LyricsMapper`. Deux décisions confirmées avec Nicolas avant rédaction (question posée, comme pour la Leçon 31) : interop Swift/C++ directe (pas de pont Objective-C++ à l'ancienne) ; périmètre limité au mécanisme seul (un appel C++ minimal qui compile et s'exécute), le portage réel étant réservé à la Leçon 36. Faits groundés via sous-agent spécialiste (Swift.org, guide swiftlang, WWDC23/25) avant rédaction : (1) réglage `"C++ and Objective-C Interoperability"` → `C++/Objective-C++` (`SWIFT_OBJC_INTEROP_MODE`, clé xcconfig corroborée par des sources secondaires, pas citée verbatim sur swift.org — nuance gardée dans la leçon), Xcode 15/Swift 5.9 minimum, largement sous le plancher actuel du projet (Xcode 26, Swift 6.0). (2) Fichiers `.hpp`/`.cpp` ajoutés via "Add Files to target", Xcode place automatiquement le `.cpp` dans Compile Sources, aucune configuration manuelle supplémentaire. (3) Point vérifié précisément car contre-intuitif : une target **App** (le cas de Karamock) a besoin d'un bridging header pour voir du C++ — contrairement à une target **Framework**, qui importe automatiquement le C++ de ses headers publics. Mécanisme réutilisé de l'interop Objective-C historique (`SWIFT_OBJC_BRIDGING_HEADER`), jamais utilisé jusqu'ici dans Karamock (projet 100% Swift). (4) Détail de grounding ajouté en aside : `CLANG_CXX_LANGUAGE_STANDARD = "gnu++20"` est déjà présent dans `project.pbxproj` avant cette leçon (vérifié) — réglage par défaut du template Xcode, ne signifie PAS que l'interop est déjà activée. Preuve minimale choisie volontairement triviale (fonction pure sur un `Int`, aucun `String`/`std::string`) pour ne pas anticiper le vrai problème de bridging de types que la Leçon 36 devra traiter avec `LyricsMapper.map(rawText:duration:)`. Point signalé comme inférence raisonnée, pas une garantie Apple : l'activation de l'interop C++ sur la target App ne devrait pas casser FactoryKit (SPM) — la règle de propagation documentée concerne des targets de package qui dépendent d'autres targets de package, pas une App qui consomme un package en simple import ; à surveiller à la compilation réelle. Nav de la Leçon 34 mise à jour pour pointer vers cette leçon. Comparée le 2026-08-03 avec l'implémentation réelle de Nicolas (commit `feat: implementation lecon 35`) : mécanisme conforme à ce qui était enseigné (`SWIFT_OBJC_INTEROP_MODE = objcxx` + `SWIFT_OBJC_BRIDGING_HEADER` bien réglés dans les deux configurations de `project.pbxproj`, ligne de preuve dans `DiscoveryView.swift` fonctionnellement identique). Un écart mineur corrigé : le dossier réel s'appelle `Engine`, pas `CppEngine` comme rédigé initialement (nom de convention personnelle, pas imposé par Apple) — extraits de code et tableau comparatif mis à jour en conséquence ; note de statut ajoutée précisant que la ligne de preuve temporaire est toujours présente (étape de retrait du scénario pas encore faite, normal à ce stade). Découverte plus importante en marge du même commit, hors périmètre de la Leçon 35 elle-même : `FullScreenPlayerView.swift` et `MiniPlayerBarTV.swift` ont aussi reçu une garde `#if os(tvOS)` autour de `.onPlayPauseCommand` (absente jusqu'ici) — remonté et corrigé dans la Leçon 34 (voir ci-dessus), pas ici, puisque c'est le code enseigné là-bas qui était fautif.
    - Le backlog de polish UI/accessibilité mis de côté en Leçon 26 (barre de nav transparente au scroll, contraste de couleurs, VoiceOver sur d'autres écrans, Reduce Motion, sections Quiz/Styles de Découvrir) est **mis de côté indéfiniment** par ce choix (pas "repris après ces deux axes") — ne pas le replanifier sans demande explicite de Nicolas.
    - `MISSION.md` mis à jour ("Success looks like" + clarification "Out of scope"), nouveau `learning-records/0008-mission-elargie-tvos-et-cpp.md` créé.
- Convention réelle du projet (vérifiée dans `Sources/Karamock/` le 2026-07-24) : Nicolas a résolu le crash de la Leçon 4 en adoptant `@Entry` (macro EnvironmentValues) + `@Environment(\.player)` (keypath) partout, plutôt que la forme par type `@Environment(PlayerState.self)` enseignée initialement. Les deux mécanismes sont valides s'ils sont utilisés de façon cohérente — suivre **sa** convention réelle (keypath/@Entry) dans les prochaines leçons plutôt que de réintroduire la forme par type.
- Nommage réel observé à respecter dans les leçons : `MockSongDownloading` (pas `MockSongDownloadService`), dossiers `Domain/`, `Services/`, `ViewModels/`, `Views/` (+ `Views/Model/` pour les structs de données).
- Retour explicite (2026-07-25) : les mauvaises réponses de quiz ne doivent jamais être nettement plus courtes/moins détaillées que la bonne — ça rend la bonne réponse devinable sans comprendre. Toujours équilibrer la longueur (nombre de mots proche) entre les 4 options, et varier la position de la bonne réponse d'une question à l'autre plutôt que de la mettre systématiquement en "a". Repris de la règle déjà présente dans le skill teach, mais pas assez appliquée en Leçon 7 — vérifier ce point pour chaque futur quiz.
- Limite connue, pas de leçon dédiée (décidé 2026-07-26) : il n'existe pas d'équivalent iOS/SwiftUI direct au `SavedStateHandle` d'Android. Sur SwiftUI, la navigation est pilotée par des valeurs (le `song` porté directement par la route), donc le problème que `SavedStateHandle` résout côté injection d'argument ne se pose pas de la même façon. Une technique existe pour supprimer la `.task` de `SongOptionsSheet` (assigner `_downloadViewModel = State(initialValue: Container.shared.songDownloadViewModel(song))` dans un `init` personnalisé plutôt que dans `.task`), mais Nicolas l'a jugée à raison cosmétique : ça ne fait que déplacer où `Container.shared` est appelé à l'intérieur de la Vue, sans résoudre le couplage Vue → Container déjà acté comme limite connue en Leçon 12. Décision : ne pas en faire une leçon, `SongOptionsSheet` garde sa `.task` actuelle.
- Statut du programme changé le 14 août 2026, à la demande explicite de Nicolas : à partir de la Leçon 43 (incluse), les leçons deviennent des leçons **bonus** — elles ne font plus vraiment partie du socle pédagogique initial (Leçons 1 à 42, qui reste la référence structurée), mais sont faites pour construire progressivement un produit réel, étape par étape, au fil des besoins plutôt que suivre un programme planifié à l'avance. `MISSION.md` mis à jour en conséquence (nouvelle puce sous "Success looks like"). Kickers des Leçons 43-45 mis à jour pour porter la mention "Bonus" (ex. "Leçon 43 · Bonus — Audit de performance (1/3)"), convention à reprendre pour toute leçon suivante à partir de maintenant.
- Nouvel axe — Audit de performance (démarré le 14 août 2026, à la demande explicite de Nicolas : « je veux que tu m'écrives un guide pour corriger ces points pas à pas et m'apprendre à les tester »). Suite de `audits/2026-08-14-performance-audit-complet.md` (relu en début de session), qui remplace celui du 8 août et confirme que ses 3 problèmes (rendu synchrone MainActor, allocations non poolées, absence de cache de glyphes) sont corrigés — les deux correctifs directs du 10 août (`learning-records/2026-08-10-fix-lyrics-engine-batch1.md`, `2026-08-10-fix-pixelbuffer-pooling.md`) l'avaient déjà fait sans passer par une leçon formelle. Cette fois, à la demande de Nicolas, les 7 points restants sont enseignés plutôt qu'appliqués directement, groupés en 3 leçons calquées sur les 3 phases déjà proposées par l'audit lui-même :
  - Leçon 43 (écrite le 14 août 2026, pas encore faite par Nicolas) — Phase 1 : le frame-skip, seul point 🔴 critique de l'audit. `LyricsEngineView` calcule une `VisualKey` (index de ligne active + progression de transition arrondie au 1/60e + dimensions du canvas) dérivée de `lyrics`/`currentTime` côté Swift, et `.task(id: request)` devient `.task(id: visualKey)` — `request` (temps exact en ms) reste utilisé tel quel à l'intérieur de `renderCurrent()`, seule la fréquence de déclenchement devient grossière. Deux écarts assumés par rapport à l'exemple littéral de l'audit : (1) `width`/`height` gardés dans la clé, sans quoi une rotation d'écran hors transition ne redéclencherait jamais de rendu ; (2) `transitionDuration = 0.5` dupliqué depuis `LyricsPage.cpp` (Leçon 41), signalé comme point de synchronisation manuelle à surveiller. Introduit Instruments Time Profiler — jamais utilisé jusqu'ici dans ce projet — comme méthode de vérification principale (source : WWDC26 "Profile, fix, and verify: Improve app responsiveness with Instruments"), avec un compteur `print()` temporaire en complément rapide.
  - **Révision de la Leçon 43 (même jour, 14 août 2026, avant toute implémentation)** : Nicolas a challengé le frame-skip proposé — il repose sur une liste fermée de ce qui peut visuellement changer (ligne active + progression), alors que Karamock prévoit des arrière-plans animés, des effets d'accentuation et une animation continue de la progression, tous des cas où l'image change à chaque frame sans transition de ligne. Voir `learning-records/0009-nicolas-repere-le-conflit-frame-skip-temps-reel.md`. La Leçon 43 est réécrite (kicker/h1/subtitle changés dans la forme, contenu remplacé) : plus de `VisualKey`, le rendu continu à chaque tick reste un choix assumé, le coalescing du 10 août (`isRendering`/`pendingRequest`) reste valable tel quel (backpressure, pas une décision de contenu), et le levier de perf devient le coût par frame — porté par les Leçons 44 (cache de largeur) et 45 (blend scanline), toutes deux inchangées sur le fond. Instruments reste la méthode de vérification principale, mais sert désormais à mesurer si le budget par frame est tenu (≈8,3 ms à 120 Hz) une fois 44/45 appliquées, pas à confirmer une baisse de fréquence de rendu. `minimumInterval` sur `TimelineView` documenté comme dernier recours conditionnel (si dépassement mesuré), jamais appliqué par défaut. `audits/2026-08-14-performance-audit-complet.md` révisé en parallèle : §2 réécrit, tableau de bord (ligne "Boucle de rendu" 4/10 🔴 → 6/10 🟡 "à vérifier"), table de priorisation réordonnée (cache de largeur + blend scanline passent en priorité 1/2, l'ancien frame-skip devient "vérification Instruments du budget" en priorité 3), roadmap avec une nouvelle Phase 1bis dédiée à cette vérification.
  - Leçon 44 (écrite le 14 août 2026, pas encore faite par Nicolas) — Phase 2 : `LazyVStack` dans `PlaylistDetailView` (49 chansons dans "Best Of Rock", header hors du lazy stack), cache de largeur mesurée dans `TextRenderer` (`widthCache_`, `std::map<std::pair<std::string,int>,int>`, clé `(texte, hauteur arrondie)` — écart assumé : une vraie table de mémoïsation plutôt que l'invalidation manuelle suggérée par l'audit, elle se stabilise d'elle-même à 2× le nombre de lignes de la chanson), et le fix `sizeInBytes()` d'une ligne (`pixels_.size()` → `width_ * height_ * 4`). Trois méthodes de vérification différentes enseignées côte à côte : compter les instanciations de `SongRow` via un `print` temporaire en tête de `body`, instrumenter la branche de cache-miss (insiste sur le fait qu'un test de non-régression prouve la correction du résultat mais jamais qu'un cache est réellement utilisé), et un vrai test rouge/vert (`KaramockTests/PixelBufferTests.swift`, nouveau fichier, `karamock.PixelBuffer` utilisé directement) pour `sizeInBytes()`. Aside vérifié empiriquement dans `project.pbxproj` (pas deviné) : `KaramockTests` n'a ni `SWIFT_OBJC_BRIDGING_HEADER` ni `SWIFT_OBJC_INTEROP_MODE` réglés (seul le target `Karamock` les a) — ça fonctionne quand même car `KaramockTests` est un target hébergé (`TEST_HOST` → `Karamock.app`), qui importe le module `Karamock` déjà compilé plutôt que de compiler du C++ brut lui-même.
  - Leçon 45 (écrite le 14 août 2026, pas encore faite par Nicolas) — Phase 3 : blend par scanline (`PixelBuffer::blendScanline` remplace `blendPixel`, retiré car plus aucun appelant — même logique que le retrait de `LyricsView.swift` en Leçon 42), `durationInSeconds` devenue propriété stockée de `Song` (calculée dans un `init` custom, déménagée de `Song+Duration.swift` vers `Song.swift` — Swift n'autorise pas de propriété stockée dans une extension ; précisé que ça ne défait PAS la Leçon 24, les deux fichiers étant déjà côté Domain), et `Set<Song.ID>` pour la déduplication dans `InMemoryDownloadedSongsRepository`. Point le plus substantiel : le passage au `Set` referme la recommandation « suggérée, non appliquée » de la Leçon 26 (`nonisolated var id`) — `knownIDs.insert(song.id)`, contrairement à l'ancien `storage.contains(song)` qui ne lisait jamais `id` (ignorée par la comparaison `Hashable` synthétisée puisque calculée, Leçon 30), force une vraie lecture de `song.id` depuis l'acteur ; sans `nonisolated`, ce fichier ne compilerait pas sans `await` supplémentaire. Les 2 `await song.id` de `CachedLyricsRepository` (Leçon 26) et le `@MainActor` de `SongIdentityTests` (Leçon 30) deviennent inutiles et sont retirés au même endroit. Introduit `@Test(arguments:)` (tests paramétrés) — première utilisation dans ce projet.
  - Les trois leçons ont été écrites en relisant le code source réel (`Engine/`, `Views/Player/FullScreenPlayer/`, `Domain/Models/Song.swift`, `Repositories/`) avant rédaction, pas depuis l'audit seul — les écarts avec l'audit littéral listés ci-dessus sont assumés explicitement dans les leçons plutôt que suivis mot pour mot.
- **Pivot le 14 août 2026, même jour, à la demande explicite de Nicolas** : dans une session séparée (nouvelle demande, pas une correction post-implémentation des Leçons 43-45 ci-dessus), Nicolas a fait relire le chemin chaud C++/Swift (`TextRenderer.cpp`, `PixelBuffer.cpp`, `LyricsRenderEngine.swift`) avec un prompt dédié qu'il a lui-même rédigé en collaboration (rôle « prompt engineer » demandé explicitement), puis a demandé un guide pour appliquer les points trouvés. Cette relecture indépendante a recoupé 4 des 6 points déjà couverts par les Leçons 43-45 (cache de largeur/kerning, `sizeInBytes()`, blend), et en a trouvé 2 nouveaux absents de l'audit du 14 août : `Data(count:)` réalloué à chaque frame côté Swift (`LyricsRenderEngine.swift`, jamais poolé contrairement à `PixelBuffer`), et `Font::verticalMetrics()` recalculé à chaque frame alors qu'il ne dépend que de la police (invariante après chargement). Après avoir signalé le recoupement à Nicolas plutôt que de dupliquer silencieusement le contenu, décision prise : **les Leçons 43, 44 et 45 sont supprimées** (fichiers `lessons/004{3,4,5}-*.html` retirés, nav de la Leçon 42 restaurée à « à venir », `RESOURCES.md` nettoyé des sources devenues orphelines côté LazyVStack/`Set`/`SE-0434`/coalescing `task(id:)`, celles encore pertinentes reformulées sans référence de leçon), remplacées par un guide autonome, **`audits/2026-08-14-guide-migration-performance.md`**, qui couvre les 6 points du chemin chaud C++/Swift sans dépendre d'aucune leçon ni du gabarit `/teach` (pas de quiz, pas de `mission-tie`, format checklist pratique). `MISSION.md` mis à jour avec une puce de révision expliquant la pause. **Consigne explicite de Nicolas, à respecter tant qu'il ne dit pas le contraire** : ne plus garder la structure du skill `/teach` (gabarit leçon HTML numéroté) pour cet axe bonus jusqu'à nouvel ordre — le socle Leçons 1-42 n'est pas concerné, seul l'axe "produit incrémental" démarré en Leçon 43 l'est. Si une future demande similaire arrive, produire directement un document pratique (guide/audit) plutôt qu'une leçon, sans redemander confirmation à chaque fois.
